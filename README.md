### 必要ツールのインストール
- `gem install solargraph`
- `brew install cask`
- `brew install multimarkdown`

### 初期化コマンド
- `cd ~/.emacs.d`
- `cask install`

### パッケージのインストール方法
- `M-x pallet-install`
- `M-x pallet-update`
